# Rust
/target
**/*.rs.bk
# (Cargo.lock IS committed for binary apps — intentionally not ignored)

# Python prototype
__pycache__/
*.pyc
.venv/
venv/

# Whisper models (large; download on first run)
*.bin
*.ggml

# Embedded-asset staging (produced by CI / locally; not committed)
/vendor/
/models/

# OS
.DS_Store
Thumbs.db
